# URL of your Tool Server (Agent Tool Server / simulator)
AGENT_SERVER_URL=http://localhost:7860

# Runpod SGLang OpenAI-compatible base URL (MUST end with /v1)
AGENT_MODEL_URL=https://rbb6b3g72lf89t-30000.proxy.runpod.net/v1

# Model name exposed by SGLang
AGENT_MODEL_NAME=Qwen/Qwen3.5-35B-A3B

# Can be any string if your SGLang endpoint does not enforce auth
AGENT_API_KEY=dummy

# Parallelism for your benchmark runner
AGENT_MAX_WORKERS=1
